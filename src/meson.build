message('Add modules...')

modules = ['unit_test', 'common', 'math', 'log', 'opengl', 'window', 'image']

foreach module_name : modules
    message('\tAdd module: ' + module_name)
    subdir(module_name)
endforeach